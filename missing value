#include<stdio.h>
void main()
{
    int a[15]={12,1,13,5,6,7,9,8,5,4,2,6,1,13};
        int sum=(15*16)/2;
        int tot=0;
        for(int i=0;i<14;i++)
        {
            tot=tot+a[i];
        }
        printf("missing number is %d:",sum-tot);
}
